ext_path=$(dirname "$0")
pip install -e ${ext_path}/python/jsonvalue
pip install -e ${ext_path}/python/qrgen
chmod +x $ext_path/git/gitext
chmod +x $ext_path/brew/brewext
cp $ext_path/git/gitext /usr/local/bin
cp $ext_path/brew/brewext /usr/local/bin